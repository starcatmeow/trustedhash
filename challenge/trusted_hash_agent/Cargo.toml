[package]
name = "trusted_hash_agent"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "trusted-hash-agent"
path = "src/main.rs"

[dependencies]
trusted_hash_common = { path = "../trusted_hash_common" }
