[package]
name = "trusted_hash_attester"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "trusted-hash-attester"
path = "src/main.rs"

[dependencies]
trusted_hash_common = { path = "../trusted_hash_common" }
